<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <title><%=title%></title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/css/bootstrap.min.css">
      <!-- <link rel="stylesheet" href="https://bootswatch.com/4/cyborg/bootstrap.min.css"> -->
    </head>
    <body>
        <div class="container">
            <div class="row">
                <h1><%=title%></h1>
                <p>Preencha os campos abaixo para salvar o cleinte.</p>
                <div class="alert alert-danger col-lg-6">
                    <strong>Erro!</strong> Nome e idade são dados obrigatórios.
                </div>
            </div>
            <div class="row">
                <form action="<%=action%>" method="POST" class="form-horizontal">
                    <div class="form-group">
                        <label class="col-lg-1 control-label" for="nome">Nome:</label>
                        <div class="col-lg-5">
                            <input type="TEXT" name="nome" value="<%=doc.nome%>" class="form-control">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-lg-1 control-label" for="idade">Idade:</label>
                        <div class="col-lg-5">
                            <input type="number" name="idade" value="<%=doc.idade%>" class="form-control">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-lg-1 control-label">UF:</label>
                        <div class="col-lg-5">
                            <select name="uf" class="form-control">
                                <%const s="selected" %>
                                <option value="RS" <% if(doc.uf==="RS"){%><%=s%><%}%> >Rio Grande do Sul</option>
                                <option value="SC" <% if(doc.uf==="SC"){%><%=s%><%}%> >Santa Catarina</option>
                                <option value="PR" <% if(doc.uf==="PR"){%><%=s%><%}%> >Paraná</option>
                                <option value="SP" <% if(doc.uf==="SP"){%><%=s%><%}%> >São Paulo</option>
                            </select>
                        </div>
                        
                    </div>
                    <div class="form-group">
                        <div class="col-lg-2">
                            <a href="/" class="btn btn-default">Cancelar</a> | <input type="submit" value="Salvar" class="btn btn-primary"/>
                        </div>
                    </div> 
                </form>
            </div>
        </div>
        
    </body>
</html>